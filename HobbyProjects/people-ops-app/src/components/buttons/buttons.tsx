import React from 'react';
import '../../styles/styles.scss'; // Import the SCSS file

const Button = () => {
    return <button className="custom-button">Click Me</button>;
};

export default Button;
